# RFC-001 — Evidência de verificação

- Data: 2026-08-10
- Escopo: fundação e runtime local, sem publicação externa
- Ambiente: macOS arm64; Docker Engine 29.6.1 executando containers Linux arm64

## Fatos verificados

| Comando | Resultado real |
| --- | --- |
| `make doctor` | Toolchains obrigatórias encontradas e versões fixadas aceitas. |
| `make verify` | Sucesso: formatadores, linters, testes, builds, scanner de segredos e política do Compose. |
| `npm test` | 94 testes passaram: API 17, web 7 e contracts 70. |
| `cargo test --workspace --all-targets --locked` | 14 testes passaram; nenhum falhou. |
| `python3 -m unittest discover -s workers/model-worker/tests -v` | 9 testes passaram. |
| `python3 -m unittest discover -s scripts/tests -v` | 7 testes passaram. |
| `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | Sucesso, sem warnings. |
| `npm run build` e `cargo build --workspace --locked` | Todos os pacotes e o engine compilaram. |
| `python3 scripts/scan_secrets.py` | Sucesso; nenhum conteúdo correspondente foi impresso. |
| `python3 scripts/check_compose_policy.py` | Sucesso; limite agregado configurado de 2.684.354.560 bytes, abaixo de 4 GiB. |
| Inventário de licenças | Regeneração determinística coincidiu com o arquivo versionado: 289 pacotes de registro. |
| `make integration` | Sucesso; imagens construídas, logs JSON/correlation sem o marcador de query, migração reaplicada sem duplicar e readiness da API/engine vetado durante indisponibilidade do PostgreSQL e recuperado após restart. |
| `COMPOSE_PROJECT_NAME=ganso-market-rfc001-clean-20260810 make integration` | Sucesso sobre volume novo: primeiro boot, migration inicial, reaplicação idempotente, degradação/recuperação e shutdown. |
| Medição dos smokes | Seis containers usaram 79.590.061 bytes com o volume preservado e 78.753.395 bytes no primeiro boot isolado. |
| Encerramento do smoke | Containers e redes removidos sem órfãos; volume PostgreSQL preservado. |
| Limpeza do teste isolado | Label do projeto conferido; somente o volume e as quatro tags descartáveis foram removidos. O volume principal `ganso-market_postgres_data` permaneceu presente. |

## Limitações da evidência

- Não houve deploy nem teste no Hetzner CPX42.
- CPU e espaço em disco do host alvo não foram medidos.
- HTTPS público e autenticação pertencem à RFC-002 e não foram implementados.
- O smoke local validou Linux arm64; a arquitetura e o runtime do host alvo ainda precisam de verificação operacional.
- A evidência não inclui Yellowstone, modelos, paper trading ou qualquer caminho live.
